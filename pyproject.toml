[project]
name = "personal-assistant"
version = "0.1.0"
description = "Personal Assistant Telegram bot with Claude API integration"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.28.0",
    "python-telegram-bot>=21.0",
    "python-dotenv>=1.0.0",
    "mcp>=1.27.0",
]

